#include<stdio.h>
int main() {
	int k, i,n,a[i],sum=0;
	printf("enter the no");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("enter the k no");
	scanf("%d",&k);
	if(i<=k)
	{ 
		sum=sum+a[i];
	}
	
	printf("%d sum is",sum);
	return 0;
}
