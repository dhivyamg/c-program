#include <stdio.h>

int main() {
	int n;
	printf("enter the no.");
	scanf("%d",&n);
	if(n>0)
	printf("no is positive");
	elseif(n<0)
	printf("no is negative");
	else
	printf("no is zero");
	
	return 0;
}
