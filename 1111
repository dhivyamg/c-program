#include <stdio.h>

int main(void) {
	int a[8],n,i,k,temp;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	printf("k values");
	scanf("%d",&k);
	for(i=0;i<n;i++)
	{
		if(a[i]<a[i+1])
		{
			temp=a[i];
			a[i]=a[i+1];
			a[i+1]=temp;
		}
	}
	printf("%d",a[i]);	
	return 0;
}
